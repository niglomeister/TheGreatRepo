
function search(){
    let query = document.getElementById("query field").value;
    
    if (query != "") console.log(`searching for ${query} ...`);
    else console.log("no query provided");

    
}